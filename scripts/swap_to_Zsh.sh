#!/bin/sh

# Change shell to Zsh and set as default
chsh -s /bin/zsh

echo "\nDefault shell has been changed to Z shell :)"
echo "To change the default shell back to bash run swap_to_bash.sh (located in scripts)\n"
